spring:
  application:
    name: dubbo-provider

dubbo:
  application:
    name: dubbo-provider
  protocol:
    name: dubbo
    port: -1   # -1 表示自动分配端口（20880起）
  registry:
    address: zookeeper://zookeeper:2181
