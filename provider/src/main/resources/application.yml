spring:
  application:
    name: dubbo-provider   # ← 我叫什么名字

dubbo:
  application:
    name: dubbo-provider
  protocol:
    name: dubbo   # ← 用什么协议通信？（就是之前说的 TCP 层协议）
    port: -1   # ← 端口 -1 = 自动分配（从 20880 起）
  registry:
    address: zookeeper://zookeeper:2181   # ← 注册中心在哪？
